FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source (but will be overridden by volume mounts)
COPY . .

EXPOSE 3000

# Run in development mode with hot reload
CMD ["npm", "run", "start:dev"]